{% extends "base.html.jinja" %}

{% block content %}
    <div class="hero">
        <h1>{{ title }}</h1>
    </div>
    <div class="content">
        <section>
            <h2>CVE's I am attributed on</h2>
            <ul>
                {% for cve in cves %}
                	<li>
                    <a href="{{ cve.link }}" target="_blank">{{ cve.link_text }}</a>: {{ cve.description }}
                    </li>
                {% endfor %}
            </ul>
        </section>
        <section>
                <h2>Conferences I have spoken at</h2>
            <ul>
                <li>
                    <a href="https://bsidessf2024.sched.com/event/1abFi/your-voice-confirms-my-identity?iframe=no" target="_blank">
                        BSides San Francisco 2024</a> - Your voice confirms my identity
                </li>
            </ul>
        </section>
        <section>
            <h2>My open source projects</h2>
            A short list of my more popular / useful open source projects.<br><br>
            Total downloads for all packages listed below: {{ total }}
            <ul>
                {% for project in projects %}
                    <li>
                        <a href="{{ project.3 }}">{{ project.0 }}</a><br>
                        - Total downloads: <a href="{{ project.4 }}">{{ project.2 }}</a>
                        <br>- {{ project.1 }}
                    </li>
                {% endfor %}
            </ul>
        N.B. Totals are updated daily and include known proxies so are
            not <i>entirely</i> representative of real numbers.
        This is a PyPi limitation so I'll take whats offered.
        </section>
        <section>
            <h2>Navigation</h2>
            <a href="/">Go home</a>
        </section>
    </div>
{% endblock content %}
